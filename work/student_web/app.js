import {initializeApp} from 'https://www.gstatic.com/firebasejs/10.14.1/firebase-app.js';
import {getAuth,onAuthStateChanged,signInWithEmailAndPassword,signOut,sendPasswordResetEmail} from 'https://www.gstatic.com/firebasejs/10.14.1/firebase-auth.js';

const $=s=>document.querySelector(s);const app=$('#app');
const ADMIN_EMAIL='kalhoro.saif12@gmail.com';
const firebaseApp=initializeApp({apiKey:'AIzaSyCnnq0pc4dsjOGTUIsf_8cIhfUxRz_fvUQ',authDomain:'deft-reporter-485519-i1.firebaseapp.com',projectId:'deft-reporter-485519-i1',storageBucket:'deft-reporter-485519-i1.firebasestorage.app',messagingSenderId:'763206379422'});
const auth=getAuth(firebaseApp);let adminUser=null;
onAuthStateChanged(auth,user=>{adminUser=user?.email?.toLowerCase()===ADMIN_EMAIL?user:null;if(user&&!adminUser)signOut(auth)});
const semesterOneCourses=[
{id:'s1-fon',title:'Fundamentals of Nursing I',icon:'✚',credits:'3 credits • 2 theory • 1 skills lab',color:'#0b7f72'},
{id:'s1-anatomy',title:'Anatomy & Physiology I',icon:'⚕',credits:'3 credits • 2.5 theory • 0.5 lab',color:'#326a9c'},
{id:'s1-microbiology',title:'Microbiology',icon:'◉',credits:'2 credits • 1.5 theory • 0.5 lab',color:'#7552a3'},
{id:'s1-biochemistry',title:'Biochemistry',icon:'⬡',credits:'2 credits • theory and lab',color:'#c27924'},
{id:'s1-english',title:'Functional English',icon:'A',credits:'3 credits • communication',color:'#b64f61'},
{id:'s1-ict',title:'Information & Communication Technology',icon:'⌘',credits:'3 credits • digital skills',color:'#287c92'},
{id:'s1-pakistan',title:'Ideology & Constitution of Pakistan',icon:'★',credits:'2 credits • civic studies',color:'#527a45'}];
const semesterTwoCourses=[
{id:'s2-fundamentals-2',title:'Fundamentals of Nursing II',icon:'✚',credits:'4 credits • theory, lab and clinical',color:'#0b7f72'},
{id:'s2-quantitative-reasoning-1',title:'Quantitative Reasoning I',icon:'∑',credits:'3 credits • quantitative skills',color:'#326a9c'},
{id:'s2-anatomy-physiology-2',title:'Anatomy & Physiology II',icon:'⚕',credits:'3 credits • theory and lab',color:'#7552a3'},
{id:'s2-applied-nutrition',title:'Applied Nutrition',icon:'◉',credits:'2 credits • applied health science',color:'#c27924'},
{id:'s2-theoretical-basis-nursing',title:'Theoretical Basis of Nursing',icon:'◈',credits:'2 credits • nursing theory',color:'#b64f61'},
{id:'s2-islamic-studies-ethics',title:'Islamic Studies / Ethics',icon:'★',credits:'2 credits • ethics and values',color:'#527a45'}];
const courseCatalog={1:semesterOneCourses,2:semesterTwoCourses};
let pkg,manifest,currentSemester=1,courses=courseCatalog[1],route={page:'home'},exam=null;
const labels={lesson:'Lessons',mcq:'Practice MCQs',flashcard:'Flashcards',written_question:'Written questions',course_test:'Course tests'};
const icons={lesson:'▤',mcq:'✓',flashcard:'▱',written_question:'✎',course_test:'◷'};
const classes={lesson:'lessons',mcq:'mcqs',flashcard:'flashcards',written_question:'written',course_test:'tests'};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function titleOf(x){return x.body.title||x.body.stem||x.body.front||x.body.question||x.id}
function studyHeroVisual(){return `<div class="hero-visual" aria-label="Study pathway: learn, recall and test"><div class="clinical-mark"><svg viewBox="0 0 180 110" aria-hidden="true"><path class="heart-line" d="M8 58h28l10-23 17 51 17-37 12 9h30c9-25 34-31 46-14 13 20-5 40-46 61-34-18-53-34-49-51"/></svg><span>Clinical confidence</span></div><div class="study-flow"><div><i>01</i><span><b>Learn</b><small>Understand the concept</small></span></div><div><i>02</i><span><b>Recall</b><small>Strengthen your memory</small></span></div><div><i>03</i><span><b>Test</b><small>Practice under pressure</small></span></div></div></div>`}
function shell(content,active='learn'){app.innerHTML=`<div class="app"><header class="topbar"><div class="brand"><span class="logo">♥</span><span>NURSEPATH<br><small style="color:var(--teal)">Pakistan</small></span></div><span class="owner-pill">Semester ${currentSemester} • Published</span></header><main>${content}</main><nav class="bottom"><button class="navbtn ${active==='home'?'active':''}" data-home>⌂<br>Today</button><button class="navbtn ${active==='learn'?'active':''}" data-learn>▤<br>Learn</button><button class="navbtn" data-practice>✓<br>Practice</button></nav></div>`;bindNav()}
function bindNav(){document.querySelectorAll('[data-home]').forEach(x=>x.onclick=home);document.querySelectorAll('[data-learn]').forEach(x=>x.onclick=learn);document.querySelectorAll('[data-practice]').forEach(x=>x.onclick=()=>startExam(courses[0]));const bar=$('.topbar');if(bar&&!bar.querySelector('[data-admin]')){bar.insertAdjacentHTML('beforeend','<button class="admin-entry" data-admin aria-label="Open owner console"><span>⚙</span> Owner</button>');bar.querySelector('[data-admin]').onclick=adminPage}}
async function loadSemester(semester){const s=manifest.semesters.find(x=>x.semester===semester&&x.status==='published');if(!s)return;app.innerHTML=`<div class="splash"><div class="pulse">♥</div><p>Opening Semester ${semester}…</p></div>`;pkg=await fetch(s.packageUrl).then(r=>{if(!r.ok)throw new Error('Semester package is unavailable.');return r.json()});currentSemester=semester;courses=courseCatalog[semester]||[];learn()}
async function load(){try{manifest=await fetch(`content/manifest.json?t=${Date.now()}`).then(r=>r.json());await loadSemester(1)}catch(e){app.innerHTML=`<div class="empty"><h2>Unable to load study material</h2><p>${esc(e.message)}</p></div>`}}
function home(){shell(`<section class="hero"><div class="hero-copy"><div class="eyebrow">Your nursing journey</div><h1>Study with purpose.<br><em>Care with confidence.</em></h1><p>Build the knowledge, clinical judgment and calm decision-making that nursing practice demands.</p><button class="hero-action" data-go>Continue Semester ${currentSemester}<span>→</span></button></div>${studyHeroVisual()}</section><div class="section-head"><div><h2>Quick practice</h2><p>Train with real exam-style questions.</p></div></div><button class="module mcqs" data-exam><span class="icon">✓</span><strong>Start a 10-question test</strong><small>Timed • scored • rationales included</small></button>`,'home');$('[data-go]').onclick=learn;$('[data-exam]').onclick=()=>startExam(courses[0])}
function learn(){shell(`<section class="hero"><div class="hero-copy"><div class="eyebrow">Semester ${currentSemester} • Your study space</div><h1>Build knowledge that stays with you.</h1><p>Move from clear lessons to active recall and realistic exam practice—one focused session at a time.</p><button class="hero-action" data-start-course>Start with ${esc(courses[0]?.title||'your first course')}<span>→</span></button></div>${studyHeroVisual()}</section><div class="section-head"><div><h2>Semesters</h2><p>Choose any published semester to study.</p></div></div><div class="semester-strip">${manifest.semesters.map(s=>`<button class="sem ${s.semester===currentSemester?'active':''} ${s.status==='generating'?'generating':''}" ${s.status==='published'?`data-semester="${s.semester}"`:'disabled'}>Semester ${s.semester} • ${s.status}</button>`).join('')}</div><div class="section-head"><div><h2>Semester ${currentSemester} courses</h2><p>Choose a course and continue at your own pace.</p></div></div><div class="grid">${courses.map(c=>`<button class="course" data-course="${c.id}"><span class="course-icon" style="background:${c.color}18;color:${c.color}">${c.icon}</span><h3>${esc(c.title)}</h3><div class="course-meta">${esc(c.credits)}</div><div class="unlocked"><span>Ready to study</span><span>Open →</span></div></button>`).join('')}</div>`);$('[data-start-course]').onclick=()=>coursePage(courses[0]);document.querySelectorAll('[data-semester]').forEach(x=>x.onclick=()=>loadSemester(+x.dataset.semester));document.querySelectorAll('[data-course]').forEach(x=>x.onclick=()=>coursePage(courses.find(c=>c.id===x.dataset.course)))}
function coursePage(c){const items=pkg.items.filter(x=>x.courseId===c.id&&x.contentType!=='course_outline'),groups=Object.groupBy(items,x=>x.contentType);shell(`<button class="back" data-back>← All courses</button><section class="course-hero" style="background:linear-gradient(135deg,${c.color},var(--navy))"><div><div class="eyebrow">Semester ${currentSemester} • Unlocked</div><h1>${esc(c.title)}</h1><p>${esc(c.credits)}</p></div><div><div class="course-count">${items.length}</div><small>study items</small></div></section><div class="section-head"><div><h2>Study modules</h2><p>Choose how you want to learn today.</p></div></div><div class="module-grid">${['lesson','mcq','flashcard','written_question','course_test'].filter(t=>groups[t]?.length).map(t=>`<button class="module ${classes[t]}" data-module="${t}"><span class="icon">${icons[t]}</span><strong>${labels[t]}</strong><small>${groups[t].length} items ${t==='mcq'?'• timed test available':''}</small></button>`).join('')}</div>`,`learn`);$('[data-back]').onclick=learn;document.querySelectorAll('[data-module]').forEach(x=>x.onclick=()=>x.dataset.module==='mcq'?startExam(c):listPage(c,x.dataset.module,groups[x.dataset.module]))}
function listPage(c,type,items){shell(`<button class="back" data-back>← ${esc(c.title)}</button><div class="section-head"><div><div class="eyebrow" style="color:var(--teal)">${esc(c.title)}</div><h1>${labels[type]}</h1><p>${items.length} organized study items</p></div>${type==='mcq'?'<button class="primary" data-exam>Start test mode</button>':''}</div><div class="content-list">${items.map((x,i)=>`<button class="item" data-item="${i}"><span><b>${esc(titleOf(x))}</b><small>${esc(x.body.difficulty||x.risk.replaceAll('_',' '))}</small></span><span>→</span></button>`).join('')}</div>`);$('[data-back]').onclick=()=>coursePage(c);if($('[data-exam]'))$('[data-exam]').onclick=()=>startExam(c);document.querySelectorAll('[data-item]').forEach(x=>x.onclick=()=>reader(c,type,items,items[+x.dataset.item]))}
function reader(c,type,items,item){if(type==='lesson')return lessonReader(c,items,item);if(type==='flashcard')return flashcardReader(c,items,item);if(type==='written_question')return writtenReader(c,items,item);if(type==='course_test')return testBrief(c,items,item);genericReader(c,type,items,item)}
function lessonReader(c,items,item){const b=item.body;const list=(xs)=>`<ul class="clean-list">${(xs||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;shell(`<button class="back" data-back>← Lessons</button><article class="reader"><header class="lesson-head"><div class="eyebrow">${esc(b.unitTitle||c.title)}</div><h1>${esc(b.title||titleOf(item))}</h1><p>A guided nursing lesson with objectives, explanations and revision points.</p></header>${b.objectives?.length?`<section class="objective-box"><h2>What you will be able to do</h2>${list(b.objectives)}</section>`:''}${(b.sections||[]).map((s,i)=>`<section class="lesson-section"><span>${String(i+1).padStart(2,'0')}</span><div><h2>${esc(s.heading)}</h2><p>${esc(s.text)}</p></div></section>`).join('')}${b.keyTerms?.length?`<section><div class="section-head"><div><div class="eyebrow">Quick reference</div><h2>Key terms</h2></div></div><div class="terms">${b.keyTerms.map(x=>`<div class="term"><b>${esc(x.term)}</b><p>${esc(x.definition)}</p></div>`).join('')}</div></section>`:''}${b.summaryPoints?.length?`<section class="summary-box"><h2>Lesson summary</h2>${list(b.summaryPoints)}</section>`:''}${b.cautions?.length?`<aside class="caution"><b>Clinical safety notes</b>${list(b.cautions)}</aside>`:''}${b.sourceIds?.length?`<p class="source-line">Reviewed learning sources: ${b.sourceIds.map(esc).join(' · ')}</p>`:''}</article>`);$('[data-back]').onclick=()=>listPage(c,'lesson',items)}
function flashcardReader(c,items,item){const b=item.body;shell(`<button class="back" data-back>← Flashcards</button><article class="reader"><div class="lesson-head"><div class="eyebrow">Active recall • ${esc(b.difficulty||'study')}</div><h1>Tap the card when you are ready</h1></div><div class="flashcard-wrap"><button class="flashcard" data-reveal><small>QUESTION</small><strong>${esc(b.front)}</strong><span>Tap to reveal answer</span><div class="flash-answer"><small>ANSWER</small><strong>${esc(b.back)}</strong></div></button></div></article>`);$('[data-back]').onclick=()=>listPage(c,'flashcard',items);$('[data-reveal]').onclick=e=>e.currentTarget.classList.toggle('revealed')}
function writtenReader(c,items,item){const b=item.body,key=`nursepath-attempt-${item.id}`,saved=localStorage.getItem(key)||'';shell(`<button class="back" data-back>← Written questions</button><article class="reader written-card"><div class="eyebrow">Written practice • ${esc(b.difficulty||'study')}</div><div class="marks">${esc(b.marks||'Practice')} ${b.marks?'marks':''}</div><h1>${esc(b.question)}</h1><label class="answer-label" for="attempt">Your answer</label><textarea id="attempt" class="answer-area" placeholder="Type your attempted answer here…">${esc(saved)}</textarea><div class="exam-actions"><button class="secondary" data-save>Save my attempt</button><button class="primary" data-reveal>Reveal marking points</button></div><div class="model-answer" data-model><h2>Key points expected</h2>${b.answerPoints?.length?`<ol>${b.answerPoints.map(x=>`<li>${esc(x)}</li>`).join('')}</ol>`:'<p>No marking guide is available yet.</p>'}</div></article>`);$('[data-back]').onclick=()=>listPage(c,'written_question',items);$('[data-save]').onclick=()=>{localStorage.setItem(key,$('#attempt').value);$('[data-save]').textContent='Attempt saved ✓'};$('[data-reveal]').onclick=()=>{$('[data-model]').classList.toggle('open');$('[data-reveal]').textContent=$('[data-model]').classList.contains('open')?'Hide marking points':'Reveal marking points'}}
function testBrief(c,items,item){const b=item.body;shell(`<button class="back" data-back>← Course tests</button><article class="reader exam-brief"><div class="eyebrow">Full mock assessment</div><h1>${esc(b.title||titleOf(item))}</h1><div class="exam-facts"><div class="exam-fact"><strong>${esc(b.durationMinutes||60)}</strong><span>minutes</span></div><div class="exam-fact"><strong>${esc(b.questionCount||50)}</strong><span>questions</span></div></div>${b.instructions?.length?`<section class="objective-box"><h2>Before you begin</h2><ul class="clean-list">${b.instructions.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section>`:''}${b.blueprint?.length?`<section><div class="section-head"><div><h2>Exam blueprint</h2><p>Topics covered in this mock assessment</p></div></div><div class="blueprint">${b.blueprint.map(x=>`<div class="blueprint-row"><b>${esc(x.topic)}</b><span>${esc(x.questionCount)} questions · ${esc(x.difficulty)}</span></div>`).join('')}</div></section>`:''}<button class="primary" data-start>Start full mock exam</button></article>`);$('[data-back]').onclick=()=>listPage(c,'course_test',items);$('[data-start]').onclick=()=>startExam(c,{count:b.questionCount||50,minutes:b.durationMinutes||60})}
function genericReader(c,type,items,item){const skip=['title','stem','front','question','correctIndex'];const readable=v=>Array.isArray(v)?v.map((x,i)=>`${i+1}. ${typeof x==='string'?x:Object.values(x).join(' — ')}`).join('\n'):typeof v==='object'?Object.values(v).join(' — '):v;shell(`<button class="back" data-back>← ${labels[type]}</button><article class="reader"><div class="eyebrow">${labels[type]} • ${esc(item.body.difficulty||'study')}</div><h1>${esc(titleOf(item))}</h1>${Object.entries(item.body).filter(([k])=>!skip.includes(k)).map(([k,v])=>`<div class="field"><label>${esc(k.replaceAll('_',' '))}</label><p>${esc(readable(v))}</p></div>`).join('')}</article>`);$('[data-back]').onclick=()=>listPage(c,type,items)}
function startExam(c,settings={}){const all=pkg.items.filter(x=>x.courseId===c.id&&x.contentType==='mcq');if(!all.length)return coursePage(c);const count=Math.min(settings.count||10,all.length),questions=[...all].sort(()=>Math.random()-.5).slice(0,count),seconds=settings.minutes?settings.minutes*60:questions.length*90;exam={c,questions,index:0,answers:Array(questions.length).fill(null),seconds,timer:setInterval(()=>{exam.seconds--;const el=$('[data-time]');if(el)el.textContent=time(exam.seconds);if(exam.seconds<=0)finishExam()},1000)};renderQuestion()}
const time=s=>`${String(Math.floor(s/60)).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;
function renderQuestion(){const {c,questions,index,answers}=exam,q=questions[index];shell(`<div class="exam-shell"><button class="back" data-exit>← Exit test</button><div class="exam-top"><div><div class="eyebrow" style="color:var(--teal)">${esc(c.title)}</div><h2>Timed practice test</h2></div><div class="timer">◷ <span data-time>${time(exam.seconds)}</span></div></div><div class="progress"><i style="width:${((index+1)/questions.length)*100}%"></i></div><section class="question-card"><div class="qnum">Question ${index+1} of ${questions.length} • ${esc(q.body.difficulty||'mixed')}</div><h2>${esc(q.body.stem)}</h2><div class="options">${q.body.options.map((o,i)=>`<button class="option ${answers[index]===i?'selected':''}" data-option="${i}"><span class="letter">${'ABCD'[i]}</span><span>${esc(o)}</span></button>`).join('')}</div><div class="exam-actions"><button class="secondary" data-prev ${index===0?'disabled':''}>Previous</button><button class="primary" data-next ${answers[index]===null?'disabled':''}>${index===questions.length-1?'Finish test':'Next question'}</button></div></section></div>`,'practice');$('[data-exit]').onclick=()=>{clearInterval(exam.timer);coursePage(c)};document.querySelectorAll('[data-option]').forEach(x=>x.onclick=()=>{answers[index]=+x.dataset.option;renderQuestion()});$('[data-prev]').onclick=()=>{exam.index--;renderQuestion()};$('[data-next]').onclick=()=>index===questions.length-1?finishExam():(exam.index++,renderQuestion())}
function finishExam(){if(!exam)return;clearInterval(exam.timer);const {c,questions,answers}=exam;const correct=questions.filter((q,i)=>answers[i]===q.body.correctIndex).length,pct=Math.round(correct/questions.length*100);shell(`<div class="exam-shell"><section class="result"><div class="eyebrow" style="color:var(--teal)">Test completed</div><h1>${pct>=80?'Excellent clinical recall!':pct>=60?'Good progress—keep practicing.':'Let’s strengthen the foundations.'}</h1><div class="score-ring" style="--score:${pct*3.6}deg"><strong>${pct}%</strong></div><p>${correct} correct out of ${questions.length}</p><button class="primary" data-again>Try another test</button><button class="secondary" data-course>Return to course</button></section><div class="section-head"><div><h2>Answer review</h2><p>Understand every answer before moving on.</p></div></div>${questions.map((q,i)=>`<div class="review ${answers[i]===q.body.correctIndex?'correct':'wrong'}"><b>${i+1}. ${esc(q.body.stem)}</b><p>Your answer: ${answers[i]===null?'Not answered':esc(q.body.options[answers[i]])}</p><p><strong>Correct:</strong> ${esc(q.body.options[q.body.correctIndex])}</p><p>${esc(q.body.rationale)}</p></div>`).join('')}</div>`,'practice');$('[data-again]').onclick=()=>startExam(c);$('[data-course]').onclick=()=>coursePage(c);exam=null}
function authMessage(code){return ({'auth/invalid-credential':'The email or password is incorrect.','auth/too-many-requests':'Too many attempts. Please wait a few minutes and try again.','auth/network-request-failed':'The connection was interrupted. Please try again.'})[code]||'Sign-in could not be completed. Please try again.'}
function adminPage(){const current=auth.currentUser;if(current?.email?.toLowerCase()===ADMIN_EMAIL){adminUser=current;return adminDashboard()}shell(`<section class="admin-login"><div class="admin-login-copy"><div class="eyebrow">Private owner access</div><h1>Manage NursePath inside the app.</h1><p>Review semester publishing, content totals and operational health without opening a separate website.</p><div class="security-note"><b>Protected owner area</b><span>Your password is handled by Firebase Authentication and is never stored in the app.</span></div></div><form class="login-card" data-login-form><span class="admin-lock">⚿</span><h2>Owner sign in</h2><p>Only the approved NursePath owner account can continue.</p><label for="admin-email">Admin email</label><input id="admin-email" name="email" type="email" autocomplete="username" value="${ADMIN_EMAIL}" readonly><label for="admin-password">Password</label><input id="admin-password" name="password" type="password" autocomplete="current-password" required placeholder="Enter your password"><div class="form-message" data-login-message role="status"></div><button class="primary wide" type="submit">Open owner console</button></form></section>`,'admin');const form=$('[data-login-form]');form.onsubmit=async e=>{e.preventDefault();const button=form.querySelector('button[type="submit"]'),message=$('[data-login-message]');button.disabled=true;button.textContent='Signing in…';message.textContent='';try{const result=await signInWithEmailAndPassword(auth,ADMIN_EMAIL,form.password.value);if(result.user.email?.toLowerCase()!==ADMIN_EMAIL){await signOut(auth);throw new Error('not-owner')}adminUser=result.user;adminDashboard()}catch(error){message.textContent=authMessage(error.code);button.disabled=false;button.textContent='Open owner console'}}}
function adminDashboard(){const published=manifest.semesters.filter(s=>s.status==='published'),totalItems=published.reduce((n,s)=>n+(s.itemCount||0),0),generating=manifest.semesters.find(s=>s.status==='generating');shell(`<section class="admin-hero"><div><div class="eyebrow">NursePath owner console</div><h1>Your app, content and publishing—in one place.</h1><p>Signed in securely as ${esc(adminUser?.email||ADMIN_EMAIL)}</p></div><span class="live-badge"><i></i> System online</span></section><div class="admin-metrics"><article><span>Published semesters</span><strong>${published.length}</strong><small>Visible to students</small></article><article><span>Published study items</span><strong>${totalItems.toLocaleString()}</strong><small>Static, low-cost access</small></article><article><span>Generation</span><strong>${generating?`S${generating.semester}`:'Idle'}</strong><small>${generating?'Cloud batch in progress':'No active semester'}</small></article></div><section class="admin-panel"><div class="admin-panel-head"><div><div class="eyebrow">Content library</div><h2>Semester publishing</h2><p>Everything students can currently see in the app.</p></div><button class="secondary" data-refresh-admin>Refresh status</button></div><div class="semester-admin-list">${manifest.semesters.map(s=>`<article><span class="status-dot ${s.status}"></span><div><b>Semester ${s.semester}</b><small>${s.status==='published'?`${(s.itemCount||0).toLocaleString()} items • version ${esc(s.packageVersion||'—')}`:s.status==='generating'?'Generation is running with the cloud budget guard':'Not scheduled yet'}</small></div><span class="status-chip ${s.status}">${esc(s.status)}</span>${s.status==='published'?`<button data-open-sem="${s.semester}">View</button>`:''}</article>`).join('')}</div></section><section class="admin-grid"><article class="admin-panel"><div class="eyebrow">Owner actions</div><h2>App operations</h2><button class="admin-action" data-refresh-admin><span>↻</span><div><b>Refresh content library</b><small>Check for newly published semesters</small></div></button><button class="admin-action" data-public-view><span>◉</span><div><b>Preview student experience</b><small>See exactly what learners can access</small></div></button></article><article class="admin-panel"><div class="eyebrow">Account security</div><h2>Owner access</h2><p class="owner-email">${esc(adminUser?.email||ADMIN_EMAIL)}</p><button class="admin-action" data-reset-password><span>✉</span><div><b>Send password reset</b><small>Receive a secure reset link by email</small></div></button><button class="admin-action danger" data-sign-out><span>→</span><div><b>Sign out</b><small>Close the owner session on this device</small></div></button><div class="form-message" data-admin-message role="status"></div></article></section><aside class="admin-safety"><b>Cost-safe by design</b><p>Students read published static packages. This console does not expose Vertex AI keys or allow unprotected generation calls from the phone.</p></aside>`,'admin');document.querySelectorAll('[data-refresh-admin]').forEach(x=>x.onclick=refreshAdmin);document.querySelectorAll('[data-open-sem]').forEach(x=>x.onclick=()=>loadSemester(+x.dataset.openSem));$('[data-public-view]').onclick=learn;$('[data-sign-out]').onclick=async()=>{await signOut(auth);adminUser=null;adminPage()};$('[data-reset-password]').onclick=async()=>{const message=$('[data-admin-message]');try{await sendPasswordResetEmail(auth,ADMIN_EMAIL);message.textContent='Password reset email sent.'}catch{message.textContent='The reset email could not be sent. Please try again.'}}}
async function refreshAdmin(){const buttons=document.querySelectorAll('[data-refresh-admin]');buttons.forEach(x=>x.disabled=true);try{manifest=await fetch(`content/manifest.json?t=${Date.now()}`,{cache:'no-store'}).then(r=>r.json());adminDashboard()}catch{const message=$('[data-admin-message]');if(message)message.textContent='Status refresh failed. Please check your connection.';buttons.forEach(x=>x.disabled=false)}}
load();
